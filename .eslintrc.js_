module.exports = {
  root: true,
  extends: ['eslint:recommended', 'next'],
  ignorePatterns: ['**/payload-types.ts'],
  plugins: ['prettier'],
  parserOptions: {
    ecmaVersion: 'latest',
    sourceType: 'module',
  },

  env: {
    es6: true,
  },
};
